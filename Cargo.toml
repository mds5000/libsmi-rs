[package]
name = "libsmi-sys"
version = "0.1.0"
authors = ["matt"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
num-traits = "0.2"
num-derive = "0.2"
bitflags = "1.2"

[build-dependencies]
pkg-config = "0.3"
